{{ include('partials/header.twig') }}

<ul id="menu">
    <li data-menuanchor="firstPage"><a href="#firstPage">Slide 1</a></li>
    <li data-menuanchor="secondPage"><a href="#secondPage">Slide 2</a></li>
    <li data-menuanchor="3rdPage"><a href="#3rdPage">Slide 3</a></li>
</ul>

<div id="fullpage">
    <div class="section" id="section0">
        <h1>1</h1>
        <h1>{{chapter.name}}</h1>
        <input type="hidden" name="id" value="1" />
        <p>{{chapter.name}}</p>
    </div>
    {% for _slide in slides %}
    <div class="section" id="section{{_slide.id}}">
            <h1>{{_slide.name}}</h1>
            <input type="hidden" name="id" value="1" />
            <p>{{_slide.contents}}</p>
    </div>
    {% endfor %}
</div>

{{ include('partials/footer.twig') }}